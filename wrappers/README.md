# Grok wrapper

`grok-peer` retains its interactive and lane implementations here. Shared host, MCP,
version and socket-test support is imported from the pinned public peer-common module.
Bus access is through the public SDK only.
